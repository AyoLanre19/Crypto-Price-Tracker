<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= title || 'Crypto Price Tracker' %></title>
<link rel="stylesheet" href="/style.css" />
</head>
<body>
  <div class="background-layer"></div>
  <div class="container">
    <h1 class="main-title">💹 Crypto Price Tracker</h1>

    <form action="/price" method="POST" class="price-form" autocomplete="off">
      <div class="input-group">
        <label for="crypto">Cryptocurrency</label>
        <input
          type="text"
          id="crypto"
          name="crypto"
          placeholder="e.g. bitcoin"
          required
          spellcheck="false"
          autocomplete="off"
        />
      </div>

      <div class="input-group">
        <label for="currency">Currency</label>
        <input
          type="text"
          id="currency"
          name="currency"
          placeholder="e.g. ngn"
          required
          spellcheck="false"
          autocomplete="off"
        />
      </div>

      <div class="input-group">
        <label for="amount">Amount (default: 1)</label>
        <input
          type="number"
          id="amount"
          name="amount"
          placeholder="Amount"
          min="0.0001"
          step="any"
        />
      </div>

      <button type="submit" class="button">Check Price</button>
    </form>

    <% if (error) { %>
      <p class="error"><%= error %></p>
    <% } %>

    <footer class="footer">
      <p>Made with 💙 for crypto enthusiasts</p>
    </footer>
  </div>
</body>
</html>
